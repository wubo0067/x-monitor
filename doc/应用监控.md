# 应用监控

## 应用定义

传统的认为一个中间件实例就是一个应用，应用名是唯一的标识，有管理员定义。应用本身可以是一个进程，也可以是一组进程。x-monitor会应用对应的进程指标求和后输出。

## 配置过滤规则

1. 是否生效
2. 过滤源
3. 匹配规则
4. 附加keys
5. 对应进程的方式

## 应用的指标

## 应用采集策略

1. 过滤规则的更新
2. app的更新
3. 指标采集

